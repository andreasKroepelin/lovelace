#import "setup.typ": *
#set document(title: [Lovelace])
#show: toot-page

#title()

This is a package for writing pseudocode in #link("https://typst.app/")[Typst].
It is named after the computer science pioneer
#link("https://en.wikipedia.org/wiki/Ada_Lovelace")[Ada Lovelace] and inspired
by the #link("https://ctan.org/pkg/pseudo")[pseudo package] for LaTeX.

Pseudocode is not a programming language, it doesn't have strict syntax, so
you should be able to write it however you need to in your specific situation.
Lovelace lets you do exactly that.

On the following pages, we will explore all of Lovelace's features together.
If you are looking for a *brief overview, go #i-link("overview.typ")[here]*.
